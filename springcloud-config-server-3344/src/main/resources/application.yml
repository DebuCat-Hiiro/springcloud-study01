server:
  port: 3344

spring:
  application:
    name: spring-cloud-config-server

  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/DebuCat-Hiiro/springcloud-config.git
          force-pull: true
